
export function validationLogin(user, password){
    const promise1 = new Promise((resolve, reject) => {
        setTimeout(resolve, 500, 'one');
    });
    if(user === 'sergio' && password === '123123'){
        
    }
}